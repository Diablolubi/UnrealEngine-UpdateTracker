# For GitHub API
PyGithub

# For DeepSeek API through its OpenAI-compatible interface
openai

# For local development (reading .env files)
python-dotenv

# General purpose HTTP client (fallback)
requests
